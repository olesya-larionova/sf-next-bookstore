import Layout from "@/components/layout/layout";

export default function Blog() {
    return (
      <Layout>
        {/* ЗАГЛУШКА */}
        <div style={{marginTop: "116px", paddingTop:"200px", textAlign: "center", minHeight: "200px", backgroundColor: "lightgrey"}}>
          Блог
        </div>
      </Layout>
    );
}